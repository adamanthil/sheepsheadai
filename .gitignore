.DS_Store

__pycache__/
.venv
.cursor

node_modules/
.next

*.tsbuildinfo
next-env.d.ts

.env
# Next.js per-developer overrides (app/web/.env.local); never commit these.
.env.local
.env.*.local
app/db/node_modules/

# Training run artifacts (checkpoints, final models, plots, CSVs, per-run population)
runs/
sheepshead/analysis/card_embedding_analysis/

# Archived checkpoint backups (multi-GB model snapshots, not for version control)
model-history/

# Production deploy secrets and local backup dumps
app/deploy/.env.prod
backups/

# Playwright
app/web/test-results/
app/web/playwright-report/
